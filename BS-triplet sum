Triplet with Sum K bookmark_borderYou are given an integer array and a positive integer K. You have to tell if there exists i,j,k in the given array such that ar[i]+ar[j]+ar[k]=K, i≠j≠k.

Input Format
The first line of input contains T - the number of test cases. Its followed by 2T lines, the first line contains N and K - the size of the array and the number K. The second line contains the elements of the array.

Output Format
For each test case, print "true" if the arrays contains such elements, "false" otherwise, separated by new line.

Constraints
30 points
1 <= T <= 100
3 <= N <= 100

70 points
1 <= T <= 100
3 <= N <= 1000

General Constraints
-105 <= A[i] <= 105
0 <= K <= 105

Example
Input
3
5 60
1 20 40 100 80
12 54
12 45 52 65 21 645 234 -100 14 575 -80 112
3 15
5 5 5

Output
false
true
true


---------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------


here we use biary search to solve this question ,
the condition here is that arr[j]+arr[j]+arr[k]=k, so we have to find 3 element that gives k

we try like this,
what if we solve in this way ,that we only find one element i.e arr[k]=k-arr[i]-arr[j]

lets assume arr[k] as our target element.

t=int(input())
for _ in range(t):
    n,k=map(int,input().split())
    l=list(map(int,input().split()))
    l.sort()
    found=False
    for i in range(n-1):
        for j in range(i+1,n-1):
            target=k-l[i]-l[j]
            low=j+1
            high=n-1
            while low<=high:
                mid=(low+high)//2
                if l[mid]==target:
                    found=True
                    break
                elif l[mid]<target:
                    low=mid+1
                else:
                    high=mid-1
            if found:
                break
        if found:
            break
    print("true" if found else "false")
